# Silentium - HackTheBox Writeup

**Dificuldade:** Easy  
**Máquina:** Silentium  
**Autor:** Andrey Soares  
**Status:** Em andamento

---

## 1. Reconhecimento

Comecei realizando o reconhecimento inicial da máquina para identificar os serviços expostos.

```bash
nmap -sV -p- --min-rate 5000 <IP>
```

Portas identificadas:

- **22/tcp** - SSH
- **80/tcp** - HTTP

Após identificar o serviço web, adicionei o domínio principal no arquivo `/etc/hosts`:

```bash
sudo nano /etc/hosts
```

Entrada adicionada:

```txt
<IP> silentium.htb
```

---

## 2. Enumeração de VHOSTs

Com o domínio principal resolvendo corretamente, parti para enumeração de subdomínios/vhosts usando o Gobuster.

```bash
gobuster vhost \
  -u http://silentium.htb \
  -w /usr/share/wordlists/seclists/Discovery/DNS/subdomains-top1million-5000.txt \
  --append-domain \
  -t 50
```

Resultado encontrado:

```txt
staging.silentium.htb Status: 200
```

![Gobuster VHOST](images/08_gobuster_vhost.png)

O subdomínio encontrado foi adicionado ao `/etc/hosts`:

```txt
<IP> silentium.htb staging.silentium.htb
```

---

## 3. Análise do Subdomínio `staging.silentium.htb`

Acessando o subdomínio, encontrei uma aplicação web com fluxo de recuperação de senha.

Durante a análise das rotas da aplicação, identifiquei o endpoint de recuperação de senha:

```txt
/api/v1/account/forgot-password
```

Testei o endpoint com uma requisição `POST` enviando o e-mail do usuário alvo.

```bash
curl -s -X POST http://staging.silentium.htb/api/v1/account/forgot-password \
  -H "Content-Type: application/json" \
  -d '{"user":{"email":"ben@silentium.htb"}}' | jq
```

A resposta retornou dados sensíveis do usuário, incluindo um token temporário de redefinição de senha.

![Forgot Password Token Leak](images/09_forgot_password_token_leak.png)

Isso indica uma falha grave no fluxo de recuperação de senha: o backend não deveria retornar o token temporário diretamente na resposta da API.

---

## 4. Reset de Senha

Com o token temporário obtido, acessei a página de recuperação de senha.

![Forgot Password Page](images/10_forgot_password_page.png)

Na tela de reset, informei:

- E-mail do usuário
- Reset token obtido pela API
- Nova senha válida conforme a política da aplicação

![Reset Password Page](images/11_reset_password_page.png)

Após enviar o formulário, a senha foi redefinida com sucesso.

---

## 5. Acesso ao Flowise

Depois do reset, realizei login na aplicação com a nova senha configurada.

O painel do Flowise foi acessado com sucesso.

![Flowise Dashboard](images/12_flowise_dashboard.png)

Nesse ponto, o acesso à aplicação foi obtido como usuário autenticado.

---

## 6. Próxima Etapa: Exploração Pós-Login

Com acesso ao Flowise, o próximo objetivo é validar possíveis vetores pós-autenticação.

Pontos importantes para investigar:

- Criação de API keys
- Funcionalidades administrativas
- Integrações disponíveis
- Upload/importação de fluxos
- Execução de componentes customizados
- Variáveis e credenciais armazenadas
- Possíveis CVEs relacionados à versão da aplicação

---

## 7. Remote Code Execution no Flowise

> Seção ainda em andamento. Preencher após confirmar a exploração.

Possível estrutura para completar depois:

```markdown
## 7. Remote Code Execution - Flowise

Após obter acesso autenticado ao Flowise, identifiquei que era possível criar uma API key pela interface.

Com a API key, testei endpoints internos da aplicação e validei execução de comandos no ambiente do container.

### Evidência

![RCE Flowise](images/13_rce_flowise.png)

### Resultado

Obtive uma reverse shell dentro do container da aplicação.

Usuário no container:

```bash
whoami
```

Resultado:

```txt
root
```
```

---

## 8. Enumeração do Container

> Seção ainda em andamento.

Possível estrutura:

```markdown
## 8. Enumeração do Container

Dentro do container, enumerei variáveis de ambiente, arquivos de configuração e possíveis credenciais reutilizáveis.

```bash
env
```

```bash
env | grep -i pass
```

Durante a enumeração, encontrei uma credencial sensível em variável de ambiente.

![Container Env](images/14_container_env.png)
```

---

## 9. Movimento para o Host

> Seção ainda em andamento.

Possível estrutura:

```markdown
## 9. Movimento para o Host - Usuário ben

A senha encontrada no container foi testada via SSH contra o usuário `ben` no host.

```bash
ssh ben@silentium.htb
```

O login foi bem-sucedido, confirmando reutilização de senha entre o container e o host.

![SSH Ben](images/15_ssh_ben.png)
```

---

## 10. User Flag

> Seção ainda em andamento.

```bash
cat /home/ben/user.txt
```

```txt
<user_flag>
```

![User Flag](images/16_user_flag.png)

---

## 11. Enumeração para Privesc

> Seção ainda em andamento.

Pontos para investigar:

- Serviços locais
- Processos rodando como root
- Portas internas
- Arquivos com permissões incorretas
- Credenciais em arquivos de configuração
- Repositórios Git/Gogs
- Capabilities
- SUID binaries
- Cron jobs
- Docker/socket exposure

Comandos úteis:

```bash
id
hostname
ip a
ss -tulpn
ps aux
sudo -l
find / -perm -4000 -type f 2>/dev/null
getcap -r / 2>/dev/null
```

---

## 12. Gogs / Privesc

> Seção ainda em andamento.

Se o caminho final envolver Gogs, documentar aqui:

```markdown
## 12. Privilege Escalation - Gogs

Durante a enumeração, identifiquei o Gogs rodando localmente com privilégios elevados.

Portas encontradas:

```bash
ss -tulpn
```

Evidência:

![Gogs Ports](images/17_gogs_ports.png)

Depois, analisei a versão e possíveis vulnerabilidades associadas.

![Gogs Version](images/18_gogs_version.png)
```

---

## 13. Root Flag

> Seção final para preencher após concluir o lab.

```bash
cat /root/root.txt
```

```txt
<root_flag>
```

![Root Flag](images/19_root_flag.png)

---

## Resumo da Cadeia de Exploração

Até o momento, a cadeia foi:

```txt
Enumeração HTTP
        ↓
Descoberta de VHOST
        ↓
staging.silentium.htb
        ↓
Endpoint de forgot password expõe tempToken
        ↓
Reset de senha do usuário ben
        ↓
Login no Flowise
        ↓
Investigação pós-login
        ↓
Próxima etapa: RCE / container / host / privesc
```

---

## Observações

Este writeup está em andamento. As próximas seções devem ser preenchidas conforme o lab avançar.

Os nomes das imagens continuam a partir da imagem **08**, conforme a organização atual do repositório.

